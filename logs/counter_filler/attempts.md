## Attempt 1 — 2026-05-18
**Sub-task:** all three (counter exec bodies, filler::fill_to, main::pipeline) — small enough to attempt in one shot.
**Approach:** Ported the three counter exec bodies byte-identically from `counter_producer/counter.rs` (no spec change, no closed/open flip). For `fill_to`, adapted the snapshot+bound-preservation family from `counter_producer` to the target-bounded loop shape: loop condition `c.get() < target`, four-conjunct invariant `c.invariant() && c.value() <= target && c.bound() == old(c).bound() && target <= c.bound()`, `decreases target - c.value()`. The `target <= c.bound()` conjunct threads the precondition `target <= old(c).bound()` through the loop so that `c.value() < target` (loop guard true) combined with `target <= c.bound()` gives `c.value() < c.bound()`, discharging `incr`'s precondition. At loop exit, `!(c.get() < target)` combined with invariant `c.value() <= target` collapses to `c.value() == target`. `pipeline` is the three-call sequence (`new(target)` → `fill_to(&mut c, target)` → `c.get()`); `fill_to`'s precondition `target <= target` discharges from `new`'s ensures `c.value() == 0` plus `0 <= target`.
**Verifier output:** `verification results:: 6 verified, 0 errors`
**Next idea:** done — hand to reviewer.
