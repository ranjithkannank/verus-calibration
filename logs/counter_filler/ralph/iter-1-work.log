Done. Attempt 1 succeeded.

**Summary:**
- **Verus result:** 6 verified, 0 errors (exit 0).
- **Sub-tasks addressed (all three in one pass):**
  1. `counter.rs` exec bodies — ported byte-identically from `counter_producer/counter.rs`.
  2. `filler.rs::fill_to` — target-bounded `while c.get() < target { c.incr(); }` loop. Four-conjunct invariant `c.invariant() && c.value() <= target && c.bound() == old(c).bound() && target <= c.bound()` with `decreases target - c.value()`. Critically, this is **not** a copy of `counter_producer`'s `start + i` snapshot invariant — there's no auxiliary counter; `c.value()` itself is the progress measure.
  3. `main.rs::pipeline` — three-call composition `new(target); fill_to(&mut c, target); c.get()`.
- **Status file:** `verus_passed`.
- **Commit:** `e00fbfb` on `main`.
- **Tree state:** clean for counter_filler tracked files; remaining unstaged changes are `logs/.../orchestrator.log` (the harness updates these) and `logs/counter_filler/ralph/` (harness directory) — outside the implementer's commit scope.

Handing back to the orchestrator for review.
