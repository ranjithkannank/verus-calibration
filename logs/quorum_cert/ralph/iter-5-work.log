Attempt 5 complete. Summary:

- **Sub-task tackled:** Design step 6 — bridge `voters(qc).len()` ↔ `qc.votes.len()` under distinct voters.
- **What I added:** Internal `spec fn voter_seq`, lifted `lemma_push_to_set`, `lemma_to_set_finite`, `lemma_set_insert_new_len`, `lemma_range_nodeid_len` from `quorum_count.rs`, wrote `lemma_distinct_seq_to_set_len`, `lemma_voters_as_to_set`, and `lemma_distinct_voters_len`. Called the bridge inside a `proof { ... }` block in `verify_qc_structure` after the loop, asserted the threshold arithmetic, and the final return now verifies.
- **Result:** 9 verified, 1 error (down from 2). The only remaining failure is `lemma_qc_has_honest_voter` body (line 444), which is step 7-8 — the next sub-task. Status: `verus_failed`. Clean tree.
