Clean tree. Attempt 6 complete and committed.

## Summary

**Attempt 6 outcome: ESCALATED.**

- **Sub-task:** Wrote `logs/marzullo/escalation.md` instead of trying further variants on the Helly-1D obligation.
- **Why escalate now (only 1 prior attempt on the same obligation, not 3):** The design's own "Critical caveat" provides a constructive counterexample (`intervals=[[0,0],[10,10],[20,20]]`, `f=1`, `n=3`, `correct_at(0)=correct_at(1)=true`) that satisfies all frozen preconditions yet violates the postcondition. The "3 consecutive attempts" rule is an upper bound; with a proof of unprovability in hand, two more variants would just be flailing — and `AGENTS.md` rule #4 explicitly forbids weakening the spec.
- **What's missing:** A `correct_intervals_overlap` Helly-1D precondition linking `correct_at` to interval geometry. With it, the existing argmax proof closes immediately.
- **Verifier state:** 8 verified, 2 errors (the Helly assertion at line 292 and the dependent `marzullo` postcondition). All other machinery (counting loop, structural lemmas, main loop's trigger-aligned invariant, `lemma_max_lo_in_set`) is verified and ready to consume the fix.
- **Status file:** `escalated`. Committed as `08b30aa`.

The orchestrator should now re-invoke the architect.
