Attempt 1 complete.

**Summary:**
- **Sub-task**: design step 3 (end-to-end verify) — established baseline before touching code.
- **Action**: read AGENTS.md, the implementer role, `exercises/vec_swap_v2.rs`, and the design note. The scaffolded file committed by the operator in `7586365` already contains both the exec body (read-read-set-set) and the full multiset proof block (broadcast-use of the three `to_multiset`/multiset broadcast groups, case split on `i == j`, extensional `=~=` on the two-`update` view, per-element count assert-by).
- **Verifier output**: `verification results:: 1 verified, 0 errors`, exit 0.
- **Status**: `verus_passed` written to `logs/vec_swap_v2/status`.
- **Commit**: `6124d2e — vec_swap_v2 attempt-1: baseline; scaffold already verifies (1/0)`.

I made no edits to `exercises/vec_swap_v2.rs`. The implementer-authored diff against `spec-frozen-vec_swap_v2` for the exercise file is empty. I flagged this for the reviewer in both the attempt log and the commit message, because the invention-test framing assumes the implementer authors the proof — on this run the operator's scaffold already did so. The operator can decide whether the result still counts as an invention data point; that judgement is above my role.

`git status` shows the tree clean for `vec_swap_v2` (the one remaining unstaged change is in `logs/vec_swap/orchestrator.log`, a different exercise, untouched by me).
